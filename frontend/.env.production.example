# 프로덕션 배포 시 (Vercel 등) 반드시 설정하세요.
# Vercel: Project Settings → Environment Variables 에서 추가 후 재배포 필요.

# 백엔드 API 서버 URL (없으면 /api/users, /api/records 등 호출 시 404 발생)
VITE_API_URL=https://your-backend-api.example.com

# 기타 (필요 시 .env.development 와 동일하게 설정)
VITE_USE_MOCK=false
VITE_NAVER_MAP_CLIENT_ID=your_naver_map_client_id
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
